import React from 'react';
import { CFooter } from '@coreui/react';

const Footer = () => {
  return (
    <CFooter>
      <div>Footer</div>
    </CFooter>
  );
};

export default React.memo(Footer);
